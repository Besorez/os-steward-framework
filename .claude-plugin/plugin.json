{
  "name": "os-steward",
  "version": "0.1.0",
  "description": "Privacy-first, read-only Windows diagnostics and stewardship framework. Claude investigates system behavior through typed MCP tools — never through unrestricted shell commands. No delete, no destructive actions, no machine data in Git.",
  "author": {
    "name": "OS Steward Project"
  },
  "mcpServers": {
    "os-steward": {
      "command": "dotnet",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/src/OsSteward.Mcp/OsSteward.Mcp.csproj"
      ]
    }
  }
}
